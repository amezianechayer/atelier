// Phase 2+ : client Inngest et fonctions durables — venture/onboard (SPEC.md §8.1),
// mission/run (§8.2), nightshift/cycle (§8.3), outreach/send (§8.4).
// Approbations humaines via step.waitForEvent('action.decided', { timeout: '72h' }).
export {};
